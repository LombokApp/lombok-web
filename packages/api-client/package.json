{
  "name": "@stellariscloud/api-client",
  "main": "dist/index.js",
  "scripts": {
    "build": "bun build . --outdir dist",
    "build:clean": "bun clean && bun run build",
    "clean": "rm -rf ./dist"
  },
  "devDependencies": {
    "axios": "1.9.0"
  },
  "peerDependencies": {
    "axios": "1.9.0"
  }
}
